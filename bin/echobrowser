#!/bin/bash
set -euo pipefail
PPROC=$(ps -o comm= $PPID)

if [[ "$PPROC" == "xdg-open" ]] ; then
  # shellcheck disable=SC2046
  PPROC=$(ps -o comm= $(ps -o ppid= $PPID))
fi

(
echo "Process $PPROC wants you to browse to:" 
echo "$@"
echo ""
) > /dev/tty
